#!/bin/bash
#$1 List of WVG*.vbf files to rename
#Change WVG to WNG to file names
{
 while read input
  do
   output=$2'/'${input/WVG/WNG}
   input=$2'/'$input
   #echo $input' -> '$output
   hsi 'mv -v '$input $output';'
  done
}<$1

